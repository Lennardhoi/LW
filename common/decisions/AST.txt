foreign_politics = {
 AST_annex_nzl = {
 		icon = generic_operation
 		allowed = {
 			tag = AST
 		}
 		available = {
 			tag = AST
 			NZL = { is_ai = yes }
 			date > 1940.3.1
 		}
 		fire_only_once = yes
 		cost = 0
 		visible = {
 			country_exists = NZL
 		}
 		complete_effect = {
 			add_state_core = 641
 			add_state_core = 272
 			AST = {
 				NZL = { pow_before_annex=yes }
 				annex_country = {
 					target = NZL
 					transfer_troops = yes
 				}
 			}
			155 = {
			add_extra_state_shared_building_slots = 3
			add_building_construction = {
 					type = industrial_complex
 					level = 3
 					instant_build = yes
 				}
			}
			hidden_effect={
				if={ limit={NZL={has_completed_focus=NZL_long_range_patrol}}
				272 = {
					remove_building = {
						type = industrial_complex
						level = 2
					}
					remove_building = {
						type = arms_factory
						level = 2
					}
				}
				}
			}

 		}
 	}
 	AST_relocation= {
 		allowed = {
 			original_tag=AST
 		}
 		icon = generic_naval
 		available = {
 			tag=MIO
 		}
 		days_mission_timeout = 30	
 		fire_only_once = no
 		is_good = yes
 		activation = {
			JAP = { has_completed_focus=JAP_strike_on_the_southern_resource_area
			}
 			has_war_with = JAP
 			#always=no
 		}
 		timeout_effect = {
 			AST_relocate_mil=yes
 			AST_relocate_civ=yes
 			if={
 				limit={check_variable = {relocation<100}}
 				activate_mission=AST_relocation
 				add_to_variable= {relocation = 1}
 				#else={clear_variable=relocation}
 				}}
 		complete_effect = {
 		}
 	}
}

 

